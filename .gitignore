# Fichiers générés par Hugo (reconstruits lors du déploiement)
/public/
/resources/_gen/
/hugo_stats.json
.hugo_build.lock

# Fichiers système
.DS_Store
Thumbs.db

# Secrets locaux (JAMAIS versionnés — les clés API vont dans GitHub Secrets)
.env
.env.*
!.env.example

# Fichier d'état de diffusion (généré, persisté ailleurs)
.distribute_state.json

# Éditeurs
*.swp
*~
